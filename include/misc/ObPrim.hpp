//
// Created by Emin Tunc Kirimlioglu on 5/30/24.
//

#ifndef OBPRIM_HPP
#define OBPRIM_HPP

#include <string>
#include <sstream>

using never = std::string;
using you_up = int;
using never_gonna = std::ostringstream;
using as = std::istringstream;
using We_have = int;
using e = const std::string &;
using Your_heart_has_been_aching_but_you_ar = int;
using Inside_we_both_know_w =std::istringstream;
using both_know_wh = std::istringstream;
#define qw std::hex;
#define fh .str();

inline std::ios_base&
gonna_make_you_cry(std::ios_base& str)
{
    str.setf(std::ios_base::hex, std::ios_base::basefield);
    return str;
}
inline std::string neve(never_gonna& a) {
    return a.str();
}

inline std::ios_base&
e_gam(std::ios_base& str)
{
    str.setf(std::ios_base::hex, std::ios_base::basefield);
    return str;
}

#endif //OBPRIM_HPP